<h1><%= @article.title %></h1>
<h2><%= @article.content %></h2>

<%= link_to "Home", articles_path, class: "button-class" %>
<%= link_to 'Update Task', edit_article_path %>
<%= link_to "Delete #{@article.title}", article_path(@article), method: :delete, data: { confirm: "Are you sure?" } %>
